// Stub for incidentStore using zustand
// import { create } from 'zustand';

// export const useIncidentStore = create((set) => ({
//   incidents: [],
//   addIncident: (incident) => set((state) => ({ incidents: [...state.incidents, incident] })),
// }));
